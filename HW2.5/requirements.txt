# DATA 266 HW2.5 — GPU workstation dependencies.
#
# Install PyTorch separately, matched to the CUDA version reported by
# `nvidia-smi` on the RTX 4090 workstation, using the selector at
# https://pytorch.org/get-started/locally/ (for example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ). A version is intentionally not pinned here because the correct CUDA
# build depends on the driver actually installed on that machine.
torch>=2.2

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
