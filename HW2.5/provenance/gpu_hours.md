# RTX 4090 GPU-Hours Used

Record actual wall-clock GPU time used per session. Sum the "Duration (hours)"
column for the assignment total. Do not estimate; compute duration from the
actual start/end timestamps recorded during the session.

| Date | Session start (local time) | Session end (local time) | Duration (hours) | Activity (Part A/B/C/D/E, or re-run) | GPU UUID |
| --- | --- | --- | ---: | --- | --- |
| To be filled in | | | | | |

**Total GPU-hours used for HW2.5:** To be filled in

## Notes

<!-- Record anything relevant to GPU-hour accounting: repeated runs due to
     errors, partial thermal-test interruptions that were re-run, or shared
     workstation time with other users. -->
